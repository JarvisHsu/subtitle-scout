.serena/
node_modules/
# 不带斜杠的那条也要：符号链接不是目录，`node_modules/` 匹配不到它。
# 2026-07-30 真实事故——worktree 里 `ln -s` 指向主仓的 node_modules，那个链接被当成普通
# 文件提交了（314f95a），随后 merge 到 main 时把主仓真实的 node_modules 目录替换成一个
# 指向自己的循环链接，vitest 直接 exit 194 零输出。
node_modules
.wrangler/
.dev.vars
scratch/
scratchpad/
.env
.env.*
!.env.example
.superpowers/
dist/

# 本地部署资料（不随公开仓；本地保留）
deploy/
worker/
# 本地构建覆盖：把 image 换成 subtitle-scout:local 并补上 build 段，让领先上游的本地提交
# 能进容器（主 compose 只跟 ghcr 上游镜像，无 build 段）。纯本机部署件，见文件头注释。
docker-compose.override.yml
# 内部设计、运维、战役和研究记录不随公开仓，本地仍保留。
docs/*
!docs/agent-first-identification-architecture.md
!docs/GET_CREDENTIALS.md
!docs/GET_CREDENTIALS.en.md
!docs/tmdb-proxy-worker.js
!docs/screenshots/
!docs/screenshots/**
!docs/brand/
!docs/brand/**
# 架构图产物（Archify 生成；只入库四件套，visual-check 旁证 PNG/receipt 留本地）
!docs/diagrams/
docs/diagrams/*
!docs/diagrams/architecture.json
!docs/diagrams/architecture.html
!docs/diagrams/architecture.png
!docs/diagrams/share-card.png

# OpenSpec 变更工作区（proposal/design/tasks/spec 增量/补丁清单）同样是内部设计记录，
# 不随公开仓。仓内无任何引用：README、CI、package.json 都不提它。
openspec/
jellyfin-config/
cache/

# 本地 e2e 测试栈（scripts/gen-mock-library.sh + docker-compose.local.yml）
fixtures/media/
cache-local/
# 本地全链路 mock 库（wizard 从 0 走 + 完整字幕工作流测试栈）
local-e2e/

# v3 live matrix runner scratch (OrbStack recipe cache mount)
matrix-scratch/
sandbox-scratch/
.research/
.claude/
.omo/
.opencode/

# 本地审计、截图和监控脚本（不随公开仓；本地保留）
.cursor/
PROJECT_AUDIT_2026.md
# 内部审计快照：都是某个时点的过期快照（例：LICENSE_AUDIT 还在陈述早已修好的
# 「package.json 声明为 MIT」），行号引用也已漂移。留在工作区供本地翻阅，但不对外陈列——
# 公开仓里一份过期的许可证审计等于自曝一个不存在的问题。
LICENSE_AUDIT.md
OPENSOURCE_READINESS.md
PRIVACY_AUDIT.md
SECURITY_AUDIT_REPORT.md
monitor-loop.sh
monitor-iteration-*.png
final.png
/*.png
pnpm-lock.yaml
pnpm-workspace.yaml
DESIGN.md
web/DESIGN.md
DIRBROWSER_IMPROVEMENTS.md
DIRBROWSER_RESEARCH.md
SETTINGS_REDESIGN.md
SETTINGS_TAB_REDESIGN_SPEC.md
SETTINGS_VISUAL_MOCKUP.md
TODO.md
ZIMUKU_VISION_UI_PLAN.md
src/deployContract.test.ts
